# 03 — 非功能需求、发布与开放问题

属于《[LowLevelDetector 规格文档](README.md)》(中文整理版)。

## 1. 非功能需求

| ID | 类别 | 需求 |
|---|---|---|
| NFR-1 | 兼容性 | 目标 **minSdk 24(Android 7.0)** —— 跟着 AndroidX 的下限走。在老旧或厂商魔改的系统上,特性检测必须平稳退回(比如显示未知),不能崩溃。 |
| NFR-2 | 隐私 | 两个变体的检测都在手机本地完成;**检测结果绝不上传**。参考数据同步(FR-7)用不用网络由用户控制(FR-8,只管同步这件事)。缓存的参考数据只放在应用私有目录。**遥测看变体(FR-10)**:`firebase` 变体经 Firebase 收集崩溃报告和使用数据(默认开启,目前没有运行时关闭开关);`foss` 变体不收集任何数据。 |
| NFR-3 | 性能 | 检测异步运行;结果屏幕及时显示,检测工作绝不阻塞主线程。 |
| NFR-4 | 可靠性 | 状态按懒加载 / 缓存的规则,在界面重建和进程被杀之后还在(FR-13);刷新不死锁、不叠加(FR-5);失败被隔离在单条内(FR-6)。 |
| NFR-5 | 可维护性 | 代码库必须**人和 AI 助手都容易找到路**:状态只有一个数据来源(SSOT)、数据流单向(UDF)、**按特性放在一起** —— 一个特性的逻辑集中在一处,绝不散落到相距很远的模块。 |
| NFR-6 | 合法与安全 | 只用合法、只读的手段(FR-9);不做规避检测的花招(NG6)。 |
| NFR-7 | 可读性 | 三态模型必须让非专家(Explorer 画像)也能看懂。判定圆点(仅 Home)就是三态的视觉呈现(同一指示器):绿=最优(完美支持 / 已是最新 / 状态佳),黄=中档(有限支持 / 较新 / 状态一般),红=最差(不支持 / 陈旧 / 状态不佳)。**未知 / 无法检测通常也归入红色**,但具体归哪个颜色按每个检测项自身的语义决定,不设跨条目强制统一的映射标准(负责人裁定,2026-09-22;取代旧表述「未知绝不能看起来像成功或失败」)。 |
| NFR-8 | 自适应 | 界面用**自适应布局**:手机、平板、折叠屏、窗口缩放 / 多窗口都能用。 |

## 2. 发布范围与路线图清单

**只维护这一份正式清单** —— 原来 Part 02 §2.6、Part 04 §4.3 和本篇 §8 说的清单都指向这里,别处不再重复。

✅ 的含义:**已经实现** —— 代码有了,验收标准也验过。「待办」和「路线图」列的 ✅ 表示计划放在哪里,不代表已经做了。

| ID | 能力 | 当前 | 待办 | 路线图 |
|---|---|:---:|:---:|:---:|
| FR-1 | 特性支持检测(三态结果) | ✅ | | |
| FR-2 | 系统参数(辅助层) | ✅ | | |
| FR-13 | 进入自动检测、懒加载、缓存、恢复(含 FR-4) | ✅ | | |
| FR-5 | 下拉刷新 | ✅ | | |
| FR-6 | 单条错误隔离(**还没实现** —— 检测出错时整页会崩,见架构体检 C1;批次 2 补上) | | ✅ | |
| FR-7/16 | 服务端参考数据同步 + 离线缓存 + 版本合并 | ✅ | | |
| FR-8/17 | 网络开关(只管同步,默认关) | ✅ | | |
| FR-10 | 双变体(`foss` / `firebase`) | ✅ | | |
| FR-11/12/14/15 | 应用外壳、检测屏幕与判定圆点(2026-09-05 梳理;当前列的 ✅ 只算 FR-11/14/15 和 FR-12 的外壳布局 —— FR-12 的滚动行为和 NavigationRail 排在 Compose 批次) | ✅ | ✅ | |
| BL-7 | 运行时遥测开关(firebase) | | ✅ | |
| BL-1 | 单条详情展开(**2026-09-22 负责人定:暂不做** —— 列表卡片只保留水波纹反馈,不挂点击动作) | | ✅ | |
| BL-2 | 复制 / 分享(**两者都做** —— 负责人 2026-09-22 确认方向;实施时间与交互方式开工前再讨论) | | ✅ | |
| BL-3 | 单行刷新 | | ✅ | |
| BL-8 | 针对特殊标志组合的任务栈启动防护(FR-11 加固)—— **推迟到有用户反馈再说**(负责人 2026-09-22 裁定:先接受隐患) | | ✅ | |
| RM-1 | 导出(JSON/TOML,应用/adb;格式:Q4) | | | ✅ |
| RM-2 | 跨平台查看器(CMP) | | | ✅ |
| RM-3 | 用户自定义查询(Q8) | | | ✅ |
| RM-4 | 对外提供查询入口,如 AppFunctions(Q8) | | | ✅ |
| RM-5 | AI 原生交互 | | | ✅ |
| Q3 | 产品命名决定 | | | ✅ |

工程迁移阶段(Compose → CMP → 查看器 → 查询开放)见《[04 — 架构与决策](04-architecture-and-decisions.md)》的迁移计划。

## 3. 成功标准(产品层面)

1. Tinkerer 不用查任何外部资料,就能对目录里的每个条目回答「我的设备支持 X 吗?」。
2. 当前版本零「卡在加载」或「转屏后结果没了」的报告。
3. 成功同步一次后,开飞行模式应用照样完全可用。
4. 新贡献者(人或 AI)只看仓库目录树,几分钟内就能在一个地方找到某个检测特性的全部逻辑。

## 4. 开放问题与待定项

| # | 问题 | 是否阻塞 |
|---|---|---|
| Q1 | ~~检测条目目录(附录 A)~~ **已解决(2026-09-22)** —— 检测项本身没有问题,已预填的带顺序候选清单**按现状定稿**,即正式条目清单;Compose 改版**不会动检测项相关逻辑**。 | 已解决 |
| Q2 | **FR-7 比对语义** —— 服务端参考数据按设备 / ROM 都包含什么,对不上时怎么展示。2026-09-12 决定:代码里现在每行怎么比,先原样写进文档,批次 3 加测试防止以后改坏;批次 5b 换注册表结构时再重新设计。 | 阻塞 FR-7 细化,不阻塞整体发布。 |
| Q3 | **产品名** —— 「LowLevelDetector」还是「Android Low Level Detector」。 | 阻塞用户可见的文案,不阻塞开发。 |
| Q4 | **导出格式** —— JSON 还是 TOML。 | 只阻塞 RM-1。 |
| Q5 | ~~网络开关~~ **已解决(2026-09-05)** —— 设置「功能」组开关,**默认关闭**,当前只控制服务端 json 下载(FR-8/FR-17)。 | 已解决 |
| Q6 | ~~发布渠道~~ **已解决(2026-09-05)** —— `firebase` → Google Play + GitHub Releases;`foss` → GitHub Releases(`foss` 上 F-Droid 有用户提出,先搁置)。 | 已解决 |
| Q7 | ~~界面语言 / i18n 范围~~ **已解决(2026-09-22)** —— 目标是**尽可能支持更多语言**。当前已实装:**简体中文、繁体中文、英语、法语**;以后继续增加,也**欢迎社区贡献翻译**。 | 已解决 |
| Q8 | **自定义查询与对外暴露(RM-3/4/5)** —— 哪些基础能力可以让用户组合使用并开放给外部,系统或 AI 来访问时(比如 AppFunctions 的调用方)同意 / 安全模型怎么定。 | 只阻塞 RM-3/4/5;不影响当前版本。 |
| Q9 | ~~判定指示器命名~~ **已解决(2026-09-22)** —— **同一个指示器**:圆点就是 FR-3 三态的视觉呈现,范围**仅 Home**(Others/Prop 纯展示)。颜色语义按检测项分级:绿=最优(完美支持 / 已是最新 / 状态佳),黄=中档(有限支持 / 较新 / 状态一般),红=最差(不支持 / 陈旧 / 状态不佳;**未知 / 无法检测通常也归红**)。具体归哪个颜色**由检测项按自身语义决定,不设跨条目强制统一标准**(不搞符号主义);代码里现有的判定行为以后逐条核对,不阻塞当前措辞。 | 已解决(详见 NFR-7、FR-3、FR-15) |
| Q10 | ~~排序开关范围~~ **已解决(2026-09-12)** —— 核对过代码:「优先包名排序」开关只重排 Home 的「过期 targetSdk 应用」列表(HomeRepository.kt:1128-1144)。 | 已解决 |

## 附录 A — 检测条目目录

作为独立文档维护:《[检测条目目录](05-detection-item-catalog.md)》(状态:**已定稿(2026-09-22,Q1 解决)** —— 带顺序的候选清单按现状转为正式清单)。

- FR-1 / FR-2 在**类别级别**引用它;条目级清单只存在于目录文件里;以后新增 / 调整条目也只改这个文件。
- 条目要和检测引擎的注册表**一一对应**(见《[04 — 架构与决策](04-architecture-and-decisions.md)》的检测引擎一节)。
