<a id="R2"></a>

# R2 无 NavigationRail / 自适应布局 (FR-12 + NFR-8)

> 返回 [README 索引](../README.md) · [组4 · 已裁定, 无待办](../README.md#组4--已裁定-无待办).


**严重程度: P2(🟡, 折叠进 Compose) | 处理: 决策④**

无 `layout-sw600dp`/`layout-large` 限定符, 无 NavigationRail, 无 WindowSizeClass(复验: 纯手机布局). 与 R1 同批处理: 修订为 Compose 时代交付物 (WindowSizeClass / NavigationRail 在 Compose 中是惯用做法).

**2026-09-25 复验 (追加, 不改上文)**: 迁移已完成, Compose 侧仍无 NavigationRail / WindowSizeClass — `AppRootShell() 的 Scaffold` 是单一手机布局, 固定 `NavigationBar`(迁移决策 5 明确把 `NavigationSuiteScaffold` 列为遗留优化). 结论与裁定不变.


