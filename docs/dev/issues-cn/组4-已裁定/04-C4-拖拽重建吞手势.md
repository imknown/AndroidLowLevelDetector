<a id="C4"></a>

# C4 遗留缺陷: 拖拽中重建吞掉下拉手势 (已知遗留)

> 返回 [README 索引](../README.md) · [组4 · 已裁定, 无待办](../README.md#组4--已裁定-无待办).


**严重程度: P1(🟡) | 处理: 已裁定永久接受 (决策⑤)**

若 Activity/Fragment 重建恰好落在下拉拖拽进行中, `onRefresh` 永不触发, 没有任何请求存在, 重建后的视图展示旧列表. 手势重放的修复曾被构建, 真机验证, 又因过于侵入而回退. 影响 FR-5 AC1/AC2 的用户感知.

**处理 (决策⑤, 已批准)**: 永久接受 — "显示过期" 的一半由 AR-08 纯推导重构后的可观察设置治好; 不再排期 "待处理刷新重放" 特性.

**2026-09-22 复验 (追加, 不改上文)**: 载体已换成 Compose — 没有 Fragment, 下拉是 `MyModelListContent() 的 PullToRefreshBox` 的 `PullToRefreshBox`, 进页面由 `LaunchedEffect { viewModel.init() }` 触发. 上文那句 "治好的一半" 已经提前落地: 排序一项改由 `HomeViewModel.outdatedOrderChangeListener` 实时同步 + `onModelsLoaded() 里` 的 `onModelsLoaded` 兜底. 但 Compose 侧 "拖拽期间重建" 这个场景本身没有重新复验过. 裁定不变: 永久接受, 不排期刷新重放.


